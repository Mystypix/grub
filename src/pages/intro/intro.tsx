export const Intro = () => {
  return (
    <div>
      <h1>Intro page</h1>
      <button onClick={() => console.log("sign in")}>Sign in</button>
      <button onClick={() => console.log("sign up")}>Sign up</button>
    </div>
  );
};
